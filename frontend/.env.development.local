# Optional: force the Vite /api proxy target (restart `npm run dev` after changes).
# Leave unset — Vite probes LAN IP then 127.0.0.1 on startup (see vite.config.js).
# VITE_DEV_API_TARGET=http://127.0.0.1:5001
