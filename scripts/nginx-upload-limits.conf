# Add inside the theboutiqueline.com nginx server { } block (Hestia template custom).
# Fixes "Network Error" when uploading hero/banner images > 1MB.
client_max_body_size 15M;
proxy_read_timeout 120s;
proxy_send_timeout 120s;
